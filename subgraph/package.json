{
  "name": "cropchain-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cropchain",
    "create-local": "graph create --node http://localhost:8020/ cropchain",
    "remove-local": "graph remove --node http://localhost:8020/ cropchain",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cropchain"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.5",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
